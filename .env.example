# Supabase (optional if already in supabase.ts)
# VITE_SUPABASE_URL=https://xxxx.supabase.co
# VITE_SUPABASE_ANON_KEY=your_anon_key

# Gemini – choose one:
# Option A: Proxy (recommended) – key stays on server
VITE_USE_GEMINI_PROXY=true

# Option B: Key in browser (dev only; not for production)
# VITE_GEMINI_API_KEY=your_gemini_api_key
